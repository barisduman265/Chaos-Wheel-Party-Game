<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required so HapticFeedback impacts (medium/heavy) actually vibrate the
         device during gameplay; without it Android silently ignores them. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
    <application
        android:label="Chaos Wheel: Party Game"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- AdMob app id. MUST be a direct child of <application> (not inside
             <activity>), or MobileAdsInitProvider crashes the app at startup. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5960894143182893~3516541932"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Theme applied while the Android process starts, before the
                 Flutter UI initializes; also the window background afterward. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Use the Skia renderer instead of Impeller. On some Samsung devices
             Impeller logged "Failed to find font glyph bounds" and crashed when
             rendering text with font fallbacks (e.g. the premium pop-up), and
             also mis-rendered Turkish glyphs (ş/ı). Skia renders both reliably. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
